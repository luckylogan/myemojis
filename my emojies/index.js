//1. loop thru the array and log each of them out to the console.

const myEmojis = ["👨‍💻", "⛷", "🍲"]

for (let i = 0; i < myEmojis.length; i++) {
  console.log(myEmojis[i])
}

//2. grab the emoji container div and store it in a variable, log it to the console.

const emojiContainer = document.getElementById("emojiContainer")
